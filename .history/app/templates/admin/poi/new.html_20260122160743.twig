{% extends 'admin/layout.html.twig' %}

{% block title %}Créer un POI{% endblock %}

{% block content %}
<h1>Créer un nouveau POI</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Créer</button>
{{ form_end(form) }}

<p><a href="{{ path('admin_poi_index') }}" class="btn btn-secondary">← Retour à la liste</a></p>
{% endblock %}
